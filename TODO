add to installer:
# Disable 'begin print' and 'end print' notifications.
rm /usr/lib/cups/notifier/dbus
<build dbus>
cp dbus /usr/lib/cups/notifier/dbus
check if it works by `tail -f /var/log/cups/error_log' - nothing must be printed
