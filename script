#!/bin/sh
( printf '\e%%-12345X'
  echo @PJL JOB
  if ! echo $5 | grep -q Duplex=None; then
    echo @PJL SET DUPLEX=ON
  fi
  echo @PJL ENTER LANGUAGE=POSTSCRIPT
  cat $3
  printf '\e%%-12345X'
  echo @PJL EOJ
  printf '\e%%-12345X'
) | send-to-printer
